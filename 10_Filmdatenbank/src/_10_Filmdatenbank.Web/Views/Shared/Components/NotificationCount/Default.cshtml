@model int

@if (Model > 0)
{
    <span class="absolute -top-1 -right-1 flex h-4 w-4 items-center justify-center rounded-full bg-danger-primary text-[10px] font-bold text-white border-2 border-nav-surface shadow-lg">
        @Model
    </span>
}
