@model int

@if (Model > 0)
{
    <span class="absolute -top-1 -right-1 flex h-5 w-5 items-center justify-center rounded-full bg-danger-primary text-[10px] font-black text-white ring-2 ring-nav-bg animate-pulse">
        @(Model > 99 ? "99+" : Model.ToString())
    </span>
}
